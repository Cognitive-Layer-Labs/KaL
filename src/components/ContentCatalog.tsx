// Superseded by CoursesView (tabs + sort + wallet-gating). Kept as a re-export
// so any lingering imports resolve to the new component.
export { CoursesView as ContentCatalog } from "./CoursesView";
